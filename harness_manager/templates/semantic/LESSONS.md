# Lessons (auto-distilled + manually curated)

> Entries here outlive specific tasks. The dream cycle promotes recurring
> patterns from episodic into this file. Feel free to curate manually —
> delete bad lessons, tighten wording, reorganize sections.
>
> **This is THIS install's lessons file.** It starts with harness-invariant
> seeds below. Engagement-specific lessons accumulate beneath the seeds as
> `graduate.py` promotes candidates from the dream cycle. Cross-install
> lessons (lessons learned in OTHER installs of this harness) live in the
> upstream fork's LESSONS.md and are not synced here automatically — they
> propagate via the deferred `harness-graduate.py` flow (Step 8.4+).

## Seed lessons (harness invariants — apply to every install)
- Always read `protocols/permissions.md` before any destructive tool call.
- Write the failing test before writing the fix.
- Log to episodic memory on every significant action, success or failure.
- When a skill has failed 3+ times in 14 days, propose a rewrite.
- Never force push to protected branches under any circumstance.

## Engagement-specific lessons (auto-promoted)

<!-- graduate.py appends entries below this line -->
